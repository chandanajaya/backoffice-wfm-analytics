pandas>=2.0
numpy>=1.24

# Not currently used -- kept as a note for the "if this scaled up" extension
# path described in the README:
#   statsmodels / prophet   -> more sophisticated volume forecasting
#   pulp / ortools          -> optimization-based reallocation instead of the
#                              current rule-based greedy approach
